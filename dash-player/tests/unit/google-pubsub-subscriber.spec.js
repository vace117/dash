// import { expect } from 'chai'

describe('Google Pub/Sub Tests', () => {
  it('can import @google-cloud/pubsub', () => {
    require('@google-cloud/pubsub')

    // expect(PubSub).to.not(null)
  })
})
